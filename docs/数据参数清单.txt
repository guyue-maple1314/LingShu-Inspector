========================================================================
四足机械狗巡检系统 —— 数据参数清单（全量盘点）
来源：ros2_ws/src/inspection_bringup/config/*.yaml
     + inspection_interfaces/msg/*.msg
     + inspection_execution_cpp 各组件 constexpr 常量
说明：代码不另设验收口径，一切阈值以本清单 = 配置文件为准。
========================================================================

【一】传感器数据参数（devices.yaml）
------------------------------------------------------------------------
设备            Topic            频率/规格
IMU             /imu             1000 Hz
足力传感器      /foot_force      500 Hz（四足，足端力 ≤50N 约束）
激光雷达        /lidar_scan      （型号未定，topic 已定义）
可见光相机      /image           30 fps（左/右双目）
红外热像仪      /thermal_image   30 fps
麦克风阵列      —                8 通道
机器人 SDK      —                型号/驱动/连接方式未定（抽象接口）

【二】QoS 策略（qos.yaml）
------------------------------------------------------------------------
imu / foot_force / camera : best_effort + keep_last + depth 1（高频丢帧可容忍）
task                      : reliable + transient_local + depth 10（任务不可丢）
event                     : reliable + volatile + depth 10

【三】13 个消息的数据字段（inspection_interfaces/msg/）
------------------------------------------------------------------------
RobotState          pose / velocity / battery_level / current_gait / execution_state
OperatorInstruction source(voice/text) / raw_content
InspectionAlert     alert_type / detected_value / source_device / detection_pose
StructuredGoal      goal_id / task_type / target_pose / constraints / valid_until
Task                task_id / task_type / status / risk / benefit / urgency / progress(0..1) / estimated_consumption(%)
TaskDecision        current_task_id / ranked_task_ids / decision(EXECUTE|PREEMPT|RESUME|CHARGE|WAIT) / decision_time
EnergyConstraint    battery_level / return_distance / estimated_consumption / estimated_return_consumption / base_safe_soc_percent / safe_soc
TerrainObservation  terrain_type / pose / foot_contact[4] / velocity / confidence
CorridorState       corridor_width / centerline[] / left_boundary[] / right_boundary[] / visual_validation_passed / confidence
FusionPose          pose(PoseWithCovariance) / valid_sources[] / localization_state
SemanticAlarm       detection_event / physical_location / bim_id / slam_id / confidence
ThermalMeasurement  raw_temperature / compensated_temperature / angle / distance / emissivity / correction_factor / error_state
AcousticDiagnosis   beam_azimuth / beam_elevation / background_noise_db / output_snr_db / fault_class / confidence

【四】九项技术验收阈值（acceptance_thresholds.yaml）
------------------------------------------------------------------------
1.1  结构化指令成功率 ≥ 95%          异常切换 ≤ 500 ms
1.2  多任务成功率 92.3%              抢占 ≤ 500 ms
1.3  复杂地形通行率 > 90%            实机迁移成功率 96.8%
1.4  机器人宽 46 cm                  通道宽 75 cm
1.5  步态异常率 -85%                 均速 ≥ 0.8 m/s     单次 2 km
1.6  定位精度 ±2 cm
1.7  位置报告准确率 ≥ 98%
1.8  静态偏差 ≤ 0.2 ℃               动态偏差 ≤ 0.5 ℃
     角度范围 ±60°     距离 1–5 m    修正系数 0.95–1.05
1.9  背景噪声 85 dB                  SNR 15–20 dB       识别准确率 75%

【五】C++ 组件内置数值常量（constexpr，源码头文件）
------------------------------------------------------------------------
── 1.3 运动智能迁移 ──
观测维度        17 维（kDefaultObservationDim）
动作维度        12 维（kDefaultActionDim），每维对称范围 ±1（tanh 输出）
关节增量        ±0.04 rad/维（装配时 SetJointDeltaRangeRad 设置）
足力归一基准    50 N（观测构建时按 50N 归一，见 acceptance/tech_1_3 S5）
输出时效        >100 ms 判过期（DataFreshnessGuard 按需配置）

── 1.5 钢格网主动抑振 ──
实心地面判定    足端力 > 30 N（kSolidThreshold）
振动分级 RMS    轻微 ≥1.5 / 中度 ≥3.0 / 严重 ≥5.0（m/s²）
MPC 激活阈值    默认 Mild 及以上介入

── 1.6 弱纹理融合导航 ──
多源同步容差    5 ms（激光/双目/IMU/足力 五源）
足端力约束      ≤ 50 N
关节限位        位置 ±3.14 rad（±π），速度 ±10 rad/s
定位退化        有效源 < 2 判 Lost（kLostSourceThreshold）
                三级状态：Healthy / Degraded / Lost

── 1.7 语义地图与定位 ──
栅格分辨率      5 cm（厘米级映射，kDefaultResolutionM）
栅格融合权重    BIM 0.4 / SLAM 0.3 / 点云 0.3
位置匹配半径    30 cm（kDefaultSearchRadiusM）
置信度下限      0.3（低于判「未定位」，不虚构物理监测点）

── 1.8 动态红外测温 ──
稳像曝光        8 ms（kDefaultExposureMs）
焦距系数        400 像素/(rad·ms)
图-像-IMU同步   5 ms 容差（30fps ≈ 33ms 帧间隔）
角度有效域      −60° ~ +60°（超界标 out_of_range）
距离有效域      1 ~ 5 m
辐射率有效域    0.01 ~ 1.0（物理下限 >0）
修正系数域      0.95 ~ 1.05

── 1.9 定向声纹诊断 ──
麦克风数量      8 路（kNumMicrophones）
同步最大时延    20 ms（max_age_ns = 20,000,000）
麦克风间距      0.03 m
声速            343 m/s

── 1.2 安全电量调度（inspection_planning_py/config/battery_safety.yaml）──
安全电量基准    30%（base_safe_soc_percent）
返航余量        10 个百分点（return_reserve_percent_points）
单位距离耗电    0.01 %/m（consumption_per_meter_percent，保守初值）
动态阈值        safe_soc = max(30%, 预计返航耗电 + 10 个百分点)
策略状态        provisional_until_real_robot_calibration（实机标定后转正式）

【六】坐标系（frames.yaml，14 个）
------------------------------------------------------------------------
base_link / odom / map / lidar_link / imu_link
camera_left_link / thermal_link / microphone_link
foot_fl / foot_fr / foot_rl / foot_rr


【七】Topic / Service / Action 总表（topics.yaml）
------------------------------------------------------------------------
Topic（13）:
  /robot_state /operator_instruction /inspection_alert /structured_goal
  /task /energy_constraint /task_decision /terrain_observation
  /corridor_state /fusion_pose /semantic_alarm /thermal_measurement
  /acoustic_diagnosis
Service（2）: /validate_goal /resume_task
Action（2）:  /execute_task /navigate_goal


========================================================================
备注：
- 提升类指标（-85%、+10倍、-90% 等）需项目方提供统一基线后才能计算。
- 全部节点名见 common/node_names.hpp，话题名见 common/topic_names.hpp，
  代码中禁止散写字符串。

我们是青岛港湾职业技术学院——灵枢智行技术团队人员，技术代码均已开源上传至github
========================================================================
